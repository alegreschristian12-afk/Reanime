[project]
name = "reanime"
version = "0.1.0"
description = "Reanime FastAPI app"
requires-python = ">=3.12"
dependencies = [
    "fastapi",
    "uvicorn[standard]",
    # add any other packages your app needs here, e.g.:
    # "python-multipart",
    # "httpx",
    # "pydantic",
]

[tool.vercel]
entrypoint = "reanime:app"
